services:
  express-app:  # Name of your service
    build: .  # Builds from the Dockerfile in this directory
    container_name: express-app
    ports:
      - "8611:3000"  # Maps container port 3000 to host
    
